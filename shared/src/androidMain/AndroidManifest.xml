<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
    The App Startup `ContextInitializer` provider registration for the persistent cache moved to
    `:cache-core` (task 2.9, tasks.md). `:shared`'s own copy (left intentionally duplicated by
    Phase 1 pending this dependency wiring, per Engram #1519's CRITICAL finding) is removed here:
    `:cache-core`'s copy is now the only one on the classpath, reached transitively via
    `:shared`'s `api(projects.cacheCore)` dependency.
    -->

</manifest>
